from setuptools import setup, find_packages

setup(
    name="xlpro",
    version='0.1.0',
    packages=find_packages(),
    entry_points={
    },
    author='Daniel Evans',
    description='xlpro',
    url='tbc',
    install_requires=[],
)
