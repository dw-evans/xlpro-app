

## Build Process

Use `XLPRO_BUILD_ALL` VSCode command.

Tag release

Create new release in `xlpro_releases`

Tag release in `xlpro_releases`

Upload installer binary to `xlpro_releases` releases page on GitHub.
